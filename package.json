{
  "name": "beinatamim",
  "version": "1.0.0",
  "description": "בין הטעמים – שף פרטי ירושלים",
  "private": true,
  "scripts": {
    "build": "mkdir -p app/public && cp index.html meta.json app/ && cp -R public/. app/public/ 2>/dev/null || true && cp sitemap.xml robots.txt app/ 2>/dev/null || true"
  }
}
